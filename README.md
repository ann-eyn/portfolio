# 💻 Portfólio Pessoal - Exercício de CSS

Este projeto consiste em uma página web simples de portfólio, criada para colocar em prática os fundamentos de **HTML5** e **CSS3**. O objetivo foi explorar as diferentes formas de aplicar estilos e estruturar uma página web.

## 🚀 Tecnologias Utilizadas

* **HTML5:** Estrutura semântica (header, section, ul, li).
* **CSS3:** Estilização visual.
* **Google Fonts:** Tipografia.

## 📚 Conceitos Aplicados

Durante o desenvolvimento, foram aplicados os seguintes conceitos:

* **Tipos de Inserção de CSS:**
    * *Inline:* Uso do atributo `style` direto nas tags.
    * *Interno:* Uso da tag `<style>` no `<head>`.
    * *Externo:* Arquivo `styles.css` vinculado via `<link>`.
* **Seletores:** Uso de Classes, IDs e seletores descendentes/restritivos (ex: `.bio p`).
* **Unidades de Medida:** Uso de unidades absolutas (`px`) e relativas (`%`, `rem`).
* **Tipografia:** Importação e uso da fonte *Ubuntu* via Google Fonts.
* **Box Model:** Ajustes de `margin`, `padding` e `border`.

## 📸 Visualização

**Em andamente...**


Desenvolvido por Evelyn Sousa para fins de estudo.
